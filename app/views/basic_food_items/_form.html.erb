<%= form_for(@basic_food_item) do |f| %>
  <% if @basic_food_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@basic_food_item.errors.count, "error") %> prohibited this basic_food_item from being saved:</h2>

      <ul>
      <% @basic_food_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
