<%= simple_form_for @suggested_food_item do |f| %>
  <% if @suggested_food_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suggested_food_item.errors.count, "error") %> prohibited this suggested_food_item from being saved:</h2>

      <ul>
      <% @suggested_food_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :title %>
  </div>
  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
